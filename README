The canonical source for patterns in openSUSE now is the spec file
for the several pattern packages. The base patterns are defined in
https://build.opensuse.org/package/show/system:install:head/patterns-base

Here you can find some tools to work with patterns. At the moment
only a tool to test the minimal pattern. Just run 'make' if you are on Factory
